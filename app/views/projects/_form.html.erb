<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control input" %>
  </div>

  <div class="field form-group">
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description, class: "form-control input" %>
  </div>

  <div class="field form-group">
<%= form.number_field :user_id, id: :project_user_id, value: current_user.id, type: :hidden %>  </div>
  <div style="text-align:center;">
    <%= form.submit class:"button m-3" %>
  </div>
<% end %>
